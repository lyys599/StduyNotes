---
课程: Python学习
类型: 主题笔记
难度: 必修
预计学习时间: 80分钟
掌握状态: 未开始
tags:
  - Python
  - 数学建模
  - 函数文件与调试
---

# Pathlib与文本文件

> [!abstract] 学完本篇，你要能够
- 用 `pathlib`构造跨平台路径
- 安全读取与写入UTF-8文本
- 基于脚本位置定位项目目录

## 核心概念

- `Path`对象可用 `/`拼接
- `__file__`表示当前脚本
- 读写文本时显式指定编码

## 手敲示例

先预测输出，再逐行输入；运行成功后至少修改三个值。

```python
from pathlib import Path

project_root = Path(__file__).resolve().parents[2]
output = project_root / "tmp" / "学习记录.txt"
output.parent.mkdir(parents=True, exist_ok=True)
output.write_text("今天完成了文件读写。\n", encoding="utf-8")
print(output.read_text(encoding="utf-8"))
```

> [!example] 可运行代码
> [[python学习/13-VSCode代码/02-函数文件与调试/04_pathlib_text.py|打开对应 `.py` 文件]]。

## 代码拆解

1. 由脚本路径推导项目根目录，不依赖终端位置。
2. `mkdir`确保输出目录存在。
3. 显式UTF-8避免中文乱码。

## 数学建模中的用途

国赛附件和结果目录很多，可靠路径管理能避免“在我电脑上能跑”的问题。

## 常见报错与易错点

- 把Windows反斜杠直接写入字符串导致转义。
- 假定当前工作目录固定。
- 覆盖重要原始数据。

## 独立练习

建立 `outputs`目录，写入带当前阶段说明的文本文件，再读取并核对。

完成后去本模块的练习题笔记做“基础模仿题”；不要提前打开参考答案。

## 验收清单

- [ ] 不看示例，从空白文件写出同类程序。
- [ ] 能解释示例中每个变量的类型、形状或业务含义。
- [ ] 主动制造一次错误，并能根据报错定位问题。
- [ ] 修改输入数据后，能判断输出是否合理。
- [ ] 能用两三句话向队友解释这段代码解决了什么问题。

## 理论关联

- 本主题暂无前置理论链接。

## 下一步

返回 [[python学习/00-总导航与学习计划/Python数学建模学习总导航|Python数学建模学习总导航]]，按推荐顺序继续。
