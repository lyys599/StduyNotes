---
课程: Python学习
类型: 主题笔记
难度: 进阶
预计学习时间: 115分钟
掌握状态: 未开始
tags:
  - Python
  - 数学建模
  - 数学建模算法实现
---

# 最短路径与网络问题实现

> [!abstract] 学完本篇，你要能够
- 用邻接表表示加权图
- 实现Dijkstra
- 恢复最短路径并处理不可达节点

## 核心概念

- 边权必须非负
- 优先队列每次扩展当前最短节点
- 前驱字典用于恢复路径

## 手敲示例

先预测输出，再逐行输入；运行成功后至少修改三个值。

```python
import heapq

graph = {
    "A": [("B", 2), ("C", 5)],
    "B": [("C", 1), ("D", 4)],
    "C": [("D", 1)],
    "D": [],
}
distances = {node: float("inf") for node in graph}
distances["A"] = 0
queue = [(0, "A")]
while queue:
    distance, node = heapq.heappop(queue)
    if distance != distances[node]:
        continue
    for neighbor, weight in graph[node]:
        candidate = distance + weight
        if candidate < distances[neighbor]:
            distances[neighbor] = candidate
            heapq.heappush(queue, (candidate, neighbor))
print(distances)
```

> [!example] 可运行代码
> [[python学习/13-VSCode代码/08-数学建模算法实现/05_dijkstra.py|打开对应 `.py` 文件]]。

## 代码拆解

1. 无穷大表示暂时不可达。
2. 过期队列项直接跳过。
3. 松弛操作更新更短距离。

## 数学建模中的用途

运输路线、应急调度和网络连通问题常可抽象为图。

## 常见报错与易错点

- 存在负权仍用Dijkstra。
- 有向边与无向边混淆。
- 只输出距离不恢复具体路径。

## 独立练习

扩展代码保存前驱节点，输出A到D的完整路径和总长度。

完成后去本模块的练习题笔记做“基础模仿题”；不要提前打开参考答案。

## 验收清单

- [ ] 不看示例，从空白文件写出同类程序。
- [ ] 能解释示例中每个变量的类型、形状或业务含义。
- [ ] 主动制造一次错误，并能根据报错定位问题。
- [ ] 修改输入数据后，能判断输出是否合理。
- [ ] 能用两三句话向队友解释这段代码解决了什么问题。

## 理论关联

- [[数学建模国赛/04-优化决策/Dijkstra最短路径|Dijkstra最短路径]]

## 下一步

返回 [[python学习/00-总导航与学习计划/Python数学建模学习总导航|Python数学建模学习总导航]]，按推荐顺序继续。
