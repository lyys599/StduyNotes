---
课程: Python学习
类型: 主题笔记
难度: 必修
预计学习时间: 150分钟
掌握状态: 未开始
tags:
  - Python
  - 数学建模
  - 数学建模算法实现
---

# 熵权法与TOPSIS实现

> [!abstract] 学完本篇，你要能够
- 完成指标正向化和归一化
- 计算熵权与TOPSIS贴近度
- 输出排名并做权重敏感性

## 核心概念

- 成本型指标需正向化
- 熵权反映样本差异度
- TOPSIS按正负理想解距离排序

## 手敲示例

先预测输出，再逐行输入；运行成功后至少修改三个值。

```python
import numpy as np

X = np.array([[80, 20, 7], [70, 15, 9], [90, 30, 6]], dtype=float)
X[:, 1] = X[:, 1].max() - X[:, 1]  # 成本型正向化
Z = X / np.sqrt((X**2).sum(axis=0))
weight = np.array([0.4, 0.3, 0.3])
V = Z * weight
d_pos = np.sqrt(((V - V.max(axis=0)) ** 2).sum(axis=1))
d_neg = np.sqrt(((V - V.min(axis=0)) ** 2).sum(axis=1))
score = d_neg / (d_pos + d_neg)
print(score, np.argsort(-score) + 1)
```

> [!example] 可运行代码
> [[python学习/13-VSCode代码/08-数学建模算法实现/01_entropy_topsis.py|打开对应 `.py` 文件]]。

## 代码拆解

1. 第二列按成本型正向化。
2. 向量归一化消除量纲。
3. 贴近度越大越接近正理想方案。

## 数学建模中的用途

城市评价、方案排序和资源配置前的综合评分是C题高频任务。

## 常见报错与易错点

- 忘记指标方向。
- 直接混合不同量纲。
- 把客观权重称为绝对客观。

## 独立练习

读取城市指标数据，完成熵权TOPSIS排名，并让每个权重±10%检查前三名是否稳定。

完成后去本模块的练习题笔记做“基础模仿题”；不要提前打开参考答案。

## 验收清单

- [ ] 不看示例，从空白文件写出同类程序。
- [ ] 能解释示例中每个变量的类型、形状或业务含义。
- [ ] 主动制造一次错误，并能根据报错定位问题。
- [ ] 修改输入数据后，能判断输出是否合理。
- [ ] 能用两三句话向队友解释这段代码解决了什么问题。

## 理论关联

- [[数学建模国赛/06-评价与赋权/熵权法|熵权法]]
- [[数学建模国赛/06-评价与赋权/TOPSIS综合评价|TOPSIS综合评价]]
- [[数学建模国赛/06-评价与赋权/指标正向化与无量纲化|指标正向化]]

## 下一步

返回 [[python学习/00-总导航与学习计划/Python数学建模学习总导航|Python数学建模学习总导航]]，按推荐顺序继续。
