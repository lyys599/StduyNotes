---
课程: Python学习
类型: 主题笔记
难度: 必修
预计学习时间: 125分钟
掌握状态: 未开始
tags:
  - Python
  - 数学建模
  - 统计与机器学习
---

# 模型解释、敏感性与稳健性

> [!abstract] 学完本篇，你要能够
- 读取置换特征重要性
- 改变关键参数做敏感性分析
- 比较不同清洗和随机种子下的结论

## 核心概念

- 重要性是模型依赖而非因果效应
- 敏感性分析要给合理区间
- 稳健结论应在替代设定下保持

## 手敲示例

先预测输出，再逐行输入；运行成功后至少修改三个值。

```python
from sklearn.datasets import load_diabetes
from sklearn.ensemble import RandomForestRegressor
from sklearn.inspection import permutation_importance
from sklearn.model_selection import train_test_split

X, y = load_diabetes(return_X_y=True)
X_train, X_test, y_train, y_test = train_test_split(
    X, y, test_size=0.25, random_state=42
)
model = RandomForestRegressor(n_estimators=150, random_state=42)
model.fit(X_train, y_train)
importance = permutation_importance(
    model, X_test, y_test, n_repeats=10, random_state=42
)
print(importance.importances_mean)
```

> [!example] 可运行代码
> [[python学习/13-VSCode代码/07-统计与机器学习/10_explain_robust.py|打开对应 `.py` 文件]]。

## 代码拆解

1. 重要性在未见测试数据上计算。
2. 重复置换估计波动。
3. 重要性高只表示预测依赖该特征。

## 数学建模中的用途

论文结论不能只来自单次模型；至少做参数扰动、替代模型或样本重抽样。

## 常见报错与易错点

- 把树模型内置重要性当因果。
- 只展示支持结论的敏感性结果。
- 不同模型用了不同数据划分。

## 独立练习

改变随机种子、异常处理和模型参数，形成结论稳定性对照表。

完成后去本模块的练习题笔记做“基础模仿题”；不要提前打开参考答案。

## 验收清单

- [ ] 不看示例，从空白文件写出同类程序。
- [ ] 能解释示例中每个变量的类型、形状或业务含义。
- [ ] 主动制造一次错误，并能根据报错定位问题。
- [ ] 修改输入数据后，能判断输出是否合理。
- [ ] 能用两三句话向队友解释这段代码解决了什么问题。

## 理论关联

- [[数学建模国赛/05-机器学习/模型解释与特征重要性|模型解释与特征重要性]]
- [[数学建模国赛/07-模型检验/敏感性分析|敏感性分析]]
- [[数学建模国赛/07-模型检验/稳健性检验|稳健性检验]]

## 下一步

返回 [[python学习/00-总导航与学习计划/Python数学建模学习总导航|Python数学建模学习总导航]]，按推荐顺序继续。
