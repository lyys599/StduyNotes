---
课程: Python学习
类型: 主题笔记
难度: 必修
预计学习时间: 130分钟
掌握状态: 未开始
tags:
  - Python
  - 数学建模
  - 统计与机器学习
---

# statsmodels线性回归与结果解释

> [!abstract] 学完本篇，你要能够
- 拟合含截距的OLS
- 读取系数、区间和拟合指标
- 区分解释模型与预测模型

## 核心概念

- 系数是控制其他变量后的条件关联
- p值依赖模型假设
- 稳健标准误不修复遗漏变量和非线性

## 手敲示例

先预测输出，再逐行输入；运行成功后至少修改三个值。

```python
import numpy as np
import pandas as pd
import statsmodels.api as sm

rng = np.random.default_rng(42)
x1 = rng.uniform(0, 10, 120)
x2 = rng.normal(5, 2, 120)
y = 3 + 2 * x1 - 0.8 * x2 + rng.normal(0, 2, 120)
X = sm.add_constant(pd.DataFrame({"投入": x1, "规模": x2}))
model = sm.OLS(y, X).fit(cov_type="HC3")
print(model.params)
print(model.conf_int())
print(model.rsquared_adj)
```

> [!example] 可运行代码
> [[python学习/13-VSCode代码/07-统计与机器学习/01_ols.py|打开对应 `.py` 文件]]。

## 代码拆解

1. 显式添加截距列。
2. HC3提供异方差稳健标准误。
3. 调整R方考虑变量数量但不是预测性能。

## 数学建模中的用途

解释因素影响时用statsmodels；做泛化预测时还需留出集和交叉验证。

## 常见报错与易错点

- 把相关解释成因果。
- 只挑显著变量而不控制选择偏差。
- 不检查残差和共线性。

## 独立练习

在环境数据上拟合一个解释型回归，写出一个系数的单位化解释并检查残差图。

完成后去本模块的练习题笔记做“基础模仿题”；不要提前打开参考答案。

## 验收清单

- [ ] 不看示例，从空白文件写出同类程序。
- [ ] 能解释示例中每个变量的类型、形状或业务含义。
- [ ] 主动制造一次错误，并能根据报错定位问题。
- [ ] 修改输入数据后，能判断输出是否合理。
- [ ] 能用两三句话向队友解释这段代码解决了什么问题。

## 理论关联

- [[数学建模国赛/03-统计与预测/线性回归|线性回归]]
- [[数学建模国赛/07-模型检验/残差诊断与统计假设|残差诊断]]
- [[数学建模国赛/02-探索性分析/VIF多重共线性检测|VIF]]

## 下一步

返回 [[python学习/00-总导航与学习计划/Python数学建模学习总导航|Python数学建模学习总导航]]，按推荐顺序继续。
