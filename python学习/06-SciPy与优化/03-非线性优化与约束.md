---
课程: Python学习
类型: 主题笔记
难度: 必修
预计学习时间: 120分钟
掌握状态: 未开始
tags:
  - Python
  - 数学建模
  - SciPy与优化
---

# 非线性优化与约束

> [!abstract] 学完本篇，你要能够
- 使用 `minimize`求解有界问题
- 表达等式与不等式约束
- 用多初值检查局部最优

## 核心概念

- 非凸问题可能有多个局部解
- 初值会影响结果
- 缩放差异会影响数值稳定性

## 手敲示例

先预测输出，再逐行输入；运行成功后至少修改三个值。

```python
import numpy as np
from scipy.optimize import minimize

def objective(x):
    return (x[0] - 2) ** 2 + (x[1] - 1) ** 2

constraints = [{"type": "ineq", "fun": lambda x: x[0] + x[1] - 2}]
result = minimize(
    objective, x0=np.array([0.5, 1.5]),
    bounds=[(0, 4), (0, 4)],
    constraints=constraints, method="SLSQP",
)
if not result.success:
    raise RuntimeError(result.message)
print(result.x, result.fun)
```

> [!example] 可运行代码
> [[python学习/13-VSCode代码/06-SciPy与优化/03_nonlinear_opt.py|打开对应 `.py` 文件]]。

## 代码拆解

1. 目标函数只返回一个标量。
2. 不等式约束按 `fun(x) >= 0`解释。
3. 边界单独传入更清楚。

## 数学建模中的用途

参数估计、非线性成本、设施选址近似和多目标加权可转为非线性优化。

## 常见报错与易错点

- 只用一个初值。
- 变量量纲差数个数量级。
- 只报告目标值不检查约束。

## 独立练习

从5组随机初值求解同一非凸目标，比较结果并报告最优可行解。

完成后去本模块的练习题笔记做“基础模仿题”；不要提前打开参考答案。

## 验收清单

- [ ] 不看示例，从空白文件写出同类程序。
- [ ] 能解释示例中每个变量的类型、形状或业务含义。
- [ ] 主动制造一次错误，并能根据报错定位问题。
- [ ] 修改输入数据后，能判断输出是否合理。
- [ ] 能用两三句话向队友解释这段代码解决了什么问题。

## 理论关联

- [[数学建模国赛/04-优化决策/梯度下降法|梯度下降法]]

## 下一步

返回 [[python学习/00-总导航与学习计划/Python数学建模学习总导航|Python数学建模学习总导航]]，按推荐顺序继续。
