---
课程: Python学习
类型: 主题笔记
难度: 必修
预计学习时间: 105分钟
掌握状态: 未开始
tags:
  - Python
  - 数学建模
  - SciPy与优化
---

# 曲线拟合与参数估计

> [!abstract] 学完本篇，你要能够
- 用 `curve_fit`拟合非线性函数
- 读取参数协方差与标准误
- 画观测和拟合曲线

## 核心概念

- 模型形式来自机制或合理经验
- 初值和边界可提高稳定性
- 高拟合度不等于能外推

## 手敲示例

先预测输出，再逐行输入；运行成功后至少修改三个值。

```python
import numpy as np
from scipy.optimize import curve_fit

def growth(t, capacity, rate):
    return capacity * (1 - np.exp(-rate * t))

t = np.arange(1, 9, dtype=float)
y = np.array([18, 31, 43, 53, 61, 67, 72, 76], dtype=float)
params, covariance = curve_fit(
    growth, t, y, p0=[90, 0.25], bounds=(0, np.inf)
)
standard_errors = np.sqrt(np.diag(covariance))
print(params, standard_errors)
```

> [!example] 可运行代码
> [[python学习/13-VSCode代码/06-SciPy与优化/04_curve_fit.py|打开对应 `.py` 文件]]。

## 代码拆解

1. 拟合函数第一个参数是自变量。
2. `p0`提供合理起点。
3. 协方差对角线开方给参数标准误近似。

## 数学建模中的用途

增长曲线、衰减规律和经验关系拟合比高阶多项式更容易解释。

## 常见报错与易错点

- 参数无边界导致无意义负值。
- 样本太少拟合参数太多。
- 只画平滑曲线不画原始点。

## 独立练习

拟合指数衰减模型，计算残差、RMSE并画拟合图。

完成后去本模块的练习题笔记做“基础模仿题”；不要提前打开参考答案。

## 验收清单

- [ ] 不看示例，从空白文件写出同类程序。
- [ ] 能解释示例中每个变量的类型、形状或业务含义。
- [ ] 主动制造一次错误，并能根据报错定位问题。
- [ ] 修改输入数据后，能判断输出是否合理。
- [ ] 能用两三句话向队友解释这段代码解决了什么问题。

## 理论关联

- [[数学建模国赛/03-统计与预测/极大似然估计|极大似然估计]]

## 下一步

返回 [[python学习/00-总导航与学习计划/Python数学建模学习总导航|Python数学建模学习总导航]]，按推荐顺序继续。
